{{- $issuerEnabled := .Values.issuer.cluster }}
{{- if .Values.jxRequirements.ingress.tls.enabled }}
{{- if not .Values.jxRequirements.ingress.tls.production }}
{{- if hasKey .Values.jxRequirements.ingress "extraDomains" }}
{{- range .Values.jxRequirements.ingress.extraDomains }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "tls-{{ . | replace "." "-" }}-s"
  labels:
    jenkins.io/letsencrypt-service: staging
spec:
  secretName: "tls-{{ . | replace "." "-" }}-s"
  issuerRef:
    name: letsencrypt-staging
{{- if $issuerEnabled }}
    kind: ClusterIssuer
{{- end }}
commonName: "*.{{ . }}"
dnsNames:
  - "*.{{ . }}"
  - "{{ . }}"

---
{{- end }}
{{- end }}
{{- end }}
{{- end }}
